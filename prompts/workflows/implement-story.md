# Implement the Current Story

Implement the current assigned AI Ready PH story end to end.

1. Find the repository root from `AGENTS.md` and `package.json`. Do not search outside it.
2. Read `AGENTS.md`, `AI_CONTEXT.md`, `PROJECT_STATE.yaml`, `WORKSPACE.md`, the current story, and every linked source document.
3. Inspect existing code and tests in the affected domain.
4. State the user-visible outcome, acceptance criteria, failure cases, dependencies, and exact non-goals.
5. Write or update the smallest failing test first.
6. Make the smallest production-quality change that passes.
7. Validate authorization, tenant scope, state, idempotency, privacy, accessibility, mobile behavior, performance, and AI cost where relevant.
8. Run focused tests, then the proportional verification suite.
9. Inspect the real changed interface or boundary.
10. Update project state and logs with verified facts.

Do not commit, push, deploy, change cloud state, scan for secrets, or modify unrelated files.

Finish with the outcome, changed domains, commands and results, important decision, and exact remaining operator action.

